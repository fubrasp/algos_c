#include <stdio.h>

/*
sortStack( struct Stack* S) {
    struct Stack* tmpStack = newStack();
    while (!isEmpty(S)) {
        int tmp = pop(S);
        while (!isEmpty(tmpStack) && top(tmpStack) < tmp) {
            push(pop(tmpStack), S);
        }
        push(tmp, tmpStack);
    }
    return tmpStack;
}
*/

int main() {
    printf("Hello, World!\n");
    return 0;
}
